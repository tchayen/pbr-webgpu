export const DEBUGGING_ON = true;

export const config = {
  cubemapSize: 1024,
  irradianceMapSize: 32,
  prefilterMapSize: 256,
  brdfLutSize: 512,
  roughnessLevels: 5,
  sampleCount: 4,
  shadowMapSize: 4096,
};

export const animations = false;
